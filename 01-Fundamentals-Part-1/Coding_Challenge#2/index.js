const jonhBMI = 23.9;
const markBMI = 28.3;
let result;

if (jonhBMI > markBMI) {
  result = `Johns BMI is higher than Mak's BMI:
    Marks BMI result: ${markBMI} 🏋️‍♀️
    John's BMI result: ${jonhBMI} 🏋️‍♀️`;
  console.log(result);
} else {
  result = `Mark's BMI is higher than John's BMI:
    Marks BMI result: ${markBMI} 🏋️‍♀️
    John's BMI result: ${jonhBMI} 🏋️‍♀️`;
  console.log(result);
}
