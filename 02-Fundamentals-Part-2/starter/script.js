// Strict mode helps up to write better code.
"use strict";

let hasDriversLicence = false;
const passTest = true;

if (passTest) {
  hasDriversLicence = true;
  if (hasDriversLicence) {
    console.log("I can drive");
  }
}

const interfaces = "Audio";
